{
  "id": "express-secure-baseline",
  "name": "Express secure baseline",
  "description": "Helmet, JSON body limit, and an explicit CORS allowlist for new Express APIs.",
  "tags": ["express", "security", "api", "node"],
  "notes": "Replace ALLOWED_ORIGINS with your real frontend origins. Install: npm i helmet cors.",
  "files": [
    {
      "path": "src/middleware/security.ts",
      "operation": "create",
      "content": "import cors from \"cors\";\nimport helmet from \"helmet\";\nimport type { Express } from \"express\";\nimport express from \"express\";\n\nconst ALLOWED_ORIGINS = [\n  \"http://localhost:5173\",\n  // \"https://your-frontend.example.com\",\n];\n\nexport function applySecurityMiddleware(app: Express): void {\n  app.use(helmet());\n  app.use(express.json({ limit: \"1mb\" }));\n  app.use(\n    cors({\n      origin: ALLOWED_ORIGINS,\n      credentials: true,\n    }),\n  );\n}\n"
    }
  ]
}
